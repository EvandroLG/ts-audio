{
  "name": "demo-audio",
  "license": "MIT",
  "scripts": {
    "dev": "bun run --hot server.ts",
    "build": "bun build src/index.js --outdir=dist --minify"
  },
  "dependencies": {
    "ts-audio": "file:../.."
  }
}
